# Intentionally blank. Do not add web/node processes here.
# Platform note: No "web" dyno/process is defined here.
# Only PostgreSQL on port 5000 is expected; readiness is based on PGHOST=127.0.0.1 PGPORT=5000.
# There must be no references to port 3020; this container is PostgreSQL-only.
# Container liveness after readiness is maintained by startup.sh when KEEP_ALIVE=true (default).
