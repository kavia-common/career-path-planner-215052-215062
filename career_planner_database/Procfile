# Intentionally left blank to prevent process managers from auto-starting any web/node process.
# The database container must only run PostgreSQL via startup.sh.
# If your platform auto-detects Procfile, keeping it empty avoids accidental Node. Do not add a web: entry.
